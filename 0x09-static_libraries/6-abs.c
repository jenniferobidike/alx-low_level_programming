#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @ab: character to be checked
 *
 * Return: Absolute value of an integer
 */

int _abs(int ab)
{
	if (ab >= 0)

		return (ab);

	else

		return (-ab);
}
